#include <stdio.h>

int main () {   // int(keyword) main(Identifiers)
    printf("Hello "); // printf(Identifier)  ;(Operator)
    printf("C Tokens ! "); // printf(Identyifier) ;(operator)
    return 0;  // return(keyword) ;(Operator)

}